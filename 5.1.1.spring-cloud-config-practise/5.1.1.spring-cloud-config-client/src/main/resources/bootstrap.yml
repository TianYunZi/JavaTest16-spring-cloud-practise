spring:
  cloud:
    config:
      name: neo-config
      profile: local
#spring.cloud.config.uri=http://localhost:9096/
      label: master
      discovery:
        enabled: true
        serviceId:  spring-cloud-config-server